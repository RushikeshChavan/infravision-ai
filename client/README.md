InfraVision AI — Client

Phase 1 Vite + React + TypeScript scaffold.

Run locally:

1. Copy `.env.example` to `.env` and set `VITE_API_URL` if needed.
2. Install dependencies: `npm install`.
3. Run dev server: `npm run dev`.
