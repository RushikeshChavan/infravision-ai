InfraVision AI — Server

This folder contains the backend Phase 1 scaffold: Express + TypeScript + Mongoose connection helper.

Run locally:

1. Copy `.env.example` to `.env` and set `MONGO_URI`.
2. Install dependencies: `npm install`.
3. Run in dev: `npm run dev`.
